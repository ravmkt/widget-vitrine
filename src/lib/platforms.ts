// Arquivo não utilizado — mantido vazio para evitar erros de importação
export const PLATFORM_OPTIONS: { value: string; label: string; url: string }[] = [];

export function getPlatformLabel(_p?: string): string {
  return '';
}

export function getPlatformUrl(_p?: string): string {
  return '';
}
